apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aardvark-qa
  namespace: openshift-gitops
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: aardvark-qa
  source:
    repoURL: https://github.com/your-org/aardvark-gitops.git
    targetRevision: HEAD
    path: helm/app
    helm:
      valueFiles:
        - ../../envs/qa/values.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

